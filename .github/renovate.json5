{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  // Inherit the org-wide dependency policy for library gems (grouping, automerge, schedule, etc.).
  extends: ["github>teamshares/renovate-config:lib.json5"],
}
