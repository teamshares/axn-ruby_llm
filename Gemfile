# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# axn resolves from RubyGems per the gemspec's version constraint (>= 0.1.0-alpha.5, < 0.2.0);
# no github:/branch: override now that alpha.5 is released.

gem "lefthook", "~> 2.0" # Git-hook manager (pre-commit RuboCop on staged files)
gem "rake", "~> 13.0"
gem "rspec", "~> 3.0"
gem "rubocop", "~> 1.21"
